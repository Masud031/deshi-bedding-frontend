type Props = {
  productId: string;
};

export default function ReviewForm({
  productId,
}: Props) {

  // post review

  return (
    <section className="mt-8">

      <h2 className="text-xl font-semibold mb-5">
        Write a Review
      </h2>

      {/* form */}

    </section>
  );
}